import { headings } from "../../modules/headings";

function Settings() {
  return (
    <div>
      <h2 className="text-center text-xl font-bold ">{headings.settings}</h2>
    </div>
  );
}

export default Settings
